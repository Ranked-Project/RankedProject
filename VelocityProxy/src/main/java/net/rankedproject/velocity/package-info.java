/**
 * Main plugin entry point and initialization logic for the Velocity proxy.
 * <p>
 * Handles plugin startup, registrar execution, and instantiator initialization.
 */
package net.rankedproject.velocity;
